import { React, Component } from 'react'
import { useAlert } from 'react-alert'

export default ({props}) => {
    console.log(props)

    return(
        <div>
            <h3>en el reader...</h3>
            </div>
    )
}